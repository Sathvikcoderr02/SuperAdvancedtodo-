const config = {
  // For development
  apiBaseUrl: 'http://localhost:5000/api',
  
  // For production (uncomment and replace with your Render backend URL when needed)
  // apiBaseUrl: 'https://task-manager-api.onrender.com/api',
};

export default config;
